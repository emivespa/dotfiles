#!/usr/bin/env -S sed -f
#
# - delete gui colors

# For lines without 'cterm', replace 'gui' with 'cterm':
#
# Necessary because some colorschemes only have gui settings, such as Visual.
/cterm/!s/gui/cterm/g

# Delete gui=xxx arguments:
s/ gui[^ ]*//g

# Delete highlighting that uses fg and bg
s/ [a-z]\+=fg//g
s/ [a-z]\+=bg//g

# Remove highlight calls left empty:
/^\s*hi [^ ]* *$/d
/^\s*highlight [^ ]* *$/d
